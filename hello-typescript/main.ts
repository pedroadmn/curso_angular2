var minhaVar = 'minha variavel';

function minhaFunc (x, y) {
    return x + y;
}

//ES
let num = 2;
const PI = 3.14;

var numeros = [1, 2, 3];
numeros.map(n => n * 2);

class Matematica {
    soma(x, y){
        return x + y;
    }
}
